from __future__ import annotations
from strings import *


if __name__ == "__main__":
    pass
    # l = s.split(' ')
    # l = [x for x in l if x != '']
    # print(sum([len(x) for x in l]) / len(l))


    # test_is_rotation()
    # test_is_substring()
    # print(s.is_unique())

